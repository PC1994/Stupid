using UnityEngine;
using System.Collections;

public class PlayerAnimate : MonoBehaviour
{
	/*
	AnimatedTexture AT;

	// Use this for initialization
	void Start ()
	{
		AT = gameObject.GetComponent<AnimatedTexture> ();
	}
	
	// Update is called once per frame
	void Update ()
	{
		if (Input.GetKey ("a")) {
			AT.rowNumber = 1;
			AT.totalCells = 4;
		} else if (Input.GetKey ("d")) {
			AT.rowNumber = 1;
			AT.totalCells = 4;
		} else {
			AT.rowNumber = 0;
			AT.totalCells = 2;
		}

	}
	*/
}
